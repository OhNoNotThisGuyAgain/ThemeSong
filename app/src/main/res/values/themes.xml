<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose owns all theming at runtime (see ui/theme). These XML themes only exist to
      give the launch window the correct background and an edge-to-edge, splash-friendly shell
      before the first Compose frame.
    -->
    <style name="Theme.SpotZones" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.SpotZones.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/window_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.SpotZones</item>
    </style>
</resources>
